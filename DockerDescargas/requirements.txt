# requirements.txt — sisar/download
#
# Dependencias fijadas y verificadas para:
#   Python  3.10  (ubuntu:22.04)
#   GDAL    3.4.1 (sistema apt; binding instalado por separado en Dockerfile)
#
# Árbol de compatibilidad verificado:
#   numpy==1.23.0      wheel cp310 disponible; satisface burst2safe 1.4.5 (numpy<2.1.0)
#   Shapely==2.1.0     satisface burst2safe (>=2) y geopandas 1.0.1 (>=2.0.0)
#   burst2safe==1.4.5  única rama 1.x sin restricción aiohttp>=3.12.6 (no publicada aún)
#   rasterio==1.3.9    última versión compatible con GDAL 3.4.x
#   geopandas==1.0.1   requiere pyogrio>=0.7.2 como backend I/O principal
#   pyproj==3.3.1      compatible con libproj de ubuntu 22.04 (PROJ 8.x)
#   Fiona==1.8.21      compilado contra GDAL del sistema (--no-binary en Dockerfile)
#
# Instalación en Dockerfile (ver comentario en el paso 2):
#   pip install "GDAL==3.4.1"                        ← primero, siempre
#   pip install --no-binary rasterio,Shapely,Fiona … ← después, este archivo

# ── Core del proyecto ─────────────────────────────────────────────────────────

burst2safe==1.4.5
dem-stitcher==2.5.13
sentineleof==0.12.0

# ── Geoespacial ───────────────────────────────────────────────────────────────

rasterio==1.3.9          # --no-binary en Dockerfile (usa GDAL del sistema)
Shapely==2.1.0           # --no-binary en Dockerfile (usa GEOS del sistema)
Fiona==1.8.21            # --no-binary en Dockerfile (usa GDAL del sistema)
geopandas==1.0.1
pyogrio==0.7.2           # backend I/O requerido por geopandas 1.x
pyproj==3.3.1
affine==2.4.0

# ── Numérico / datos ──────────────────────────────────────────────────────────

numpy==1.23.0
pandas==1.4.3

# ── Red / cloud ───────────────────────────────────────────────────────────────

requests==2.28.1
aiohttp==3.9.5
boto3==1.24.28
botocore==1.27.28
s3transfer==0.6.2
jmespath==1.0.1
certifi==2022.6.15
urllib3==1.26.9
charset-normalizer==2.1.0
idna==3.3

# ── CLI / utilidades ──────────────────────────────────────────────────────────

click==8.1.3
click-plugins==1.1.1
cligj==0.7.2
tqdm==4.64.0
lxml==4.9.0
python-dateutil==2.8.2
pytz==2022.1
six==1.16.0
attrs==21.4.0
packaging==22.0

# ── Dependencias transitivas fijadas ─────────────────────────────────────────

# aiohttp
aiosignal==1.3.1
frozenlist==1.4.0
multidict==6.0.5
yarl==1.9.4
async-timeout==4.0.3     # requerido en Python < 3.11

# burst2safe / tifffile
tifffile==2022.4.22

# asf_search (dep de burst2safe)
asf-search==12.1.0
dateparser==1.1.8
tenacity==8.2.3