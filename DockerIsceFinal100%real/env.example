# Absolute path to the job working directory on the host.
# This is mounted read-write at /job inside the container.
# Must contain specification.toml and the pre-populated data/, dem/, orbits/
# directories written by the download container.
JOB_DIR=/path/to/your/job/directory
