#!/usr/bin/env bash
# PLACEHOLDER — replace with the compiled sisar-download binary before building the Docker image.
# See bin/README.md for build instructions.
echo "[sisar-download] ERROR: This is the placeholder stub. Compile the Rust binary first." >&2
echo "  cd sisar-download/ && cargo build --release && cp target/release/sisar-download bin/sisar-download" >&2
exit 1
